---
"@citypaul/dotfiles": patch
---

`delegating-github-issues` Review now creates the PR's worktree when none exists, as Land already did. A watcher on one machine can then answer comments on a PR that another machine or a cloud session opened.
